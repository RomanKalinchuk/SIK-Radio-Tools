{
  "name": "sik-radio-tools",
  "version": "1.0.1",
  "description": "Browser-based SiK telemetry radio configurator (Web Serial, static hosting)",
  "private": true,
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "node scripts/copy-assets.js",
    "watch": "tsc -w",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jimp": "^1.6.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
