# Builds sik-radio-tools and deploys to GitHub Pages.
# REQUIRED once: Settings → Pages → Source: GitHub Actions (creates the Pages site; configure-pages action removed to avoid API 404 before this is done).
# Optional: Settings → Actions → General → Workflow permissions → Read and write permissions

name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: sik-radio-tools/package-lock.json

      - name: Install and build
        working-directory: sik-radio-tools
        run: |
          npm ci
          npm run build

      - name: Prepare site
        working-directory: sik-radio-tools
        run: |
          mkdir -p _site
          cp index.html _site/
          cp -r dist _site/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: sik-radio-tools/_site

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
